<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>

	<!-- Background refresh for lecture-change notifications.
	     The identifier must match LectureMonitorScheduler.TASK_IDENTIFIER; without this entry
	     iOS refuses the registration and the switch in Settings would promise nothing again. -->
	<key>BGTaskSchedulerPermittedIdentifiers</key>
	<array>
		<string>de.fampopprol.dhbwhorb.lecture-monitor</string>
	</array>

	<key>UIBackgroundModes</key>
	<array>
		<string>fetch</string>
	</array>

	<!-- App Transport Security Settings -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<!-- Allow all HTTP connections (for development/testing) -->
		<!-- In production, you should specify exact domains instead -->
		<key>NSAllowsArbitraryLoads</key>
		<false/>

		<!-- Exception for Dualis domain (uses HTTPS, but may have certificate issues) -->
		<key>NSExceptionDomains</key>
		<dict>
			<key>dualis.dhbw.de</key>
			<dict>
				<key>NSExceptionAllowsInsecureHTTPLoads</key>
				<false/>
				<key>NSIncludesSubdomains</key>
				<true/>
				<key>NSExceptionMinimumTLSVersion</key>
				<string>TLSv1.2</string>
			</dict>
		</dict>
	</dict>

	<!-- Network Usage Description (required for iOS 14+) -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>This app needs network access to connect to the DHBW Dualis system.</string>

	<!-- Supported interface orientations for iPhone (portrait only) -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>

	<!-- Supported interface orientations for iPad (all orientations) -->
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>

---
phase: 01-secure-connectivity-hotfix
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/App.kt
  - composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/AuthenticationService.kt
autonomous: false
requirements: [SSL-06]

must_haves:
  truths:
    - "HttpClient instances in App and AuthenticationService are created via the factory"
    - "Application can connect to dualis.dhbw.de securely"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/App.kt"
      provides: "Unified HttpClient usage"
  key_links:
    - from: "App.kt"
      to: "HttpClientFactory.createHttpClient()"
      via: "Function call"
---

<objective>
Integrate the secure HttpClient factory into the application and verify connectivity to the DHBW Dualis servers.

Purpose: Finalizes the hotfix by ensuring all network requests use the trusted CA.
Output: Refactored integration and verified connectivity.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/App.kt
@composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/AuthenticationService.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate factory in App.kt and AuthenticationService.kt</name>
  <files>composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/App.kt, composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/AuthenticationService.kt</files>
  <action>
    - Replace direct 'HttpClient' instantiation in 'App.kt' with 'createHttpClient()'.
    - Ensure 'HttpCookies' and 'HttpTimeout' plugins are correctly applied (either in the common base of the factory or in 'App.kt' if the factory allows configuration).
    - Update 'AuthenticationService.createSharedHttpClient()' companion method to use 'createHttpClient()' if it's still needed, or remove it in favor of the factory.
  </action>
  <verify>
    <automated>grep "createHttpClient()" composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/App.kt</automated>
  </verify>
  <done>HttpClient factory integrated into the app</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Secure connectivity hotfix</what-built>
  <how-to-verify>
    1. Run the app on an Android emulator (especially API < 34/Android 14).
    2. Attempt to login with credentials or demo mode.
    3. Verify that the login process completes without SSL/certificate errors (like CertPathValidatorException).
    4. Run on Desktop/JVM and verify the same.
    5. Check the logs for successful GET requests to 'dualis.dhbw.de'.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
- No SSL errors when connecting to Dualis on any platform
- App can successfully authenticate and fetch data
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-connectivity-hotfix/01-03-SUMMARY.md`
</output>

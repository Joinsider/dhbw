---
phase: 01-secure-connectivity-hotfix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composeApp/src/commonMain/composeResources/files/harica_root_ca_2021.pem
  - composeApp/src/androidMain/res/xml/network_security_config.xml
  - composeApp/src/androidMain/AndroidManifest.xml
autonomous: true
requirements: [SSL-01, SSL-02]

must_haves:
  truths:
    - "HARICA Root CA file exists in resources"
    - "Android app declares network security configuration"
  artifacts:
    - path: "composeApp/src/commonMain/composeResources/files/harica_root_ca_2021.pem"
      provides: "Root CA certificate for Ktor trust"
    - path: "composeApp/src/androidMain/res/xml/network_security_config.xml"
      provides: "System-level SSL trust for Android"
  key_links:
    - from: "composeApp/src/androidMain/AndroidManifest.xml"
      to: "composeApp/src/androidMain/res/xml/network_security_config.xml"
      via: "android:networkSecurityConfig attribute"
---

<objective>
Establish the resource foundation by bundling the HARICA Root CA 2021 and configuring the Android system-level network security.

Purpose: Fixes SSL trust issues on Android < 14 and provides the CA for other platform engines.
Output: CA resource file and Android security config.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle HARICA TLS RSA Root CA 2021</name>
  <files>composeApp/src/commonMain/composeResources/files/harica_root_ca_2021.pem</files>
  <action>
    - Download the HARICA TLS RSA Root CA 2021 PEM from: https://www.harica.gr/repository/certs/HaricaTLSRSARootCA2021.pem
    - Create the directory 'composeApp/src/commonMain/composeResources/files/' if it doesn't exist.
    - Write the PEM content to 'composeApp/src/commonMain/composeResources/files/harica_root_ca_2021.pem'.
    - Use 'curl' to download or copy the content if known.
  </action>
  <verify>
    <automated>test -f composeApp/src/commonMain/composeResources/files/harica_root_ca_2021.pem && grep "BEGIN CERTIFICATE" composeApp/src/commonMain/composeResources/files/harica_root_ca_2021.pem</automated>
  </verify>
  <done>CA certificate bundled in common resources</done>
</task>

<task type="auto">
  <name>Task 2: Implement Android Network Security Config</name>
  <files>composeApp/src/androidMain/res/xml/network_security_config.xml</files>
  <action>
    - Create 'composeApp/src/androidMain/res/xml/network_security_config.xml'.
    - Configure it to trust the system-installed CAs AND the bundled HARICA CA for 'dualis.dhbw.de'.
    - Since Android doesn't directly support PEM in resources for networkSecurityConfig, we'll also copy the CA to 'composeApp/src/androidMain/res/raw/harica_root_ca_2021.cer' (binary format or renamed PEM usually works too).
  </action>
  <verify>
    <automated>test -f composeApp/src/androidMain/res/xml/network_security_config.xml</automated>
  </verify>
  <done>Network security config created</done>
</task>

<task type="auto">
  <name>Task 3: Apply config to AndroidManifest.xml</name>
  <files>composeApp/src/androidMain/AndroidManifest.xml</files>
  <action>
    - Open 'composeApp/src/androidMain/AndroidManifest.xml'.
    - Add 'android:networkSecurityConfig="@xml/network_security_config"' to the <application> tag.
  </action>
  <verify>
    <automated>grep "android:networkSecurityConfig=\"@xml/network_security_config\"" composeApp/src/androidMain/AndroidManifest.xml</automated>
  </verify>
  <done>Manifest updated to use security config</done>
</task>

</tasks>

<success_criteria>
- CA file exists in common resources
- Android network security config exists and is referenced in manifest
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-connectivity-hotfix/01-01-SUMMARY.md`
</output>

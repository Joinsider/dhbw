---
phase: 07-add-loading-animation-and-verify-desktop-documents
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified: [composeApp/src/androidMain/kotlin/de/fampopprol/dhbwhorb/util/FileViewer.android.kt, composeApp/src/iosMain/kotlin/de/fampopprol/dhbwhorb/util/FileViewer.kt]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Android implementation of saveFileWithDialog exists and uses Intent.ACTION_CREATE_DOCUMENT"
    - "iOS implementation of saveFileWithDialog exists and uses UIDocumentPickerViewController"
  artifacts:
    - path: "composeApp/src/androidMain/kotlin/de/fampopprol/dhbwhorb/util/FileViewer.android.kt"
      provides: "Android actual implementation of file saving"
    - path: "composeApp/src/iosMain/kotlin/de/fampopprol/dhbwhorb/util/FileViewer.kt"
      provides: "iOS actual implementation of file saving"
  key_links:
    - from: "FileViewer.android.kt"
      to: "ACTION_CREATE_DOCUMENT"
      via: "Intent"
    - from: "FileViewer.kt (iOS)"
      to: "UIDocumentPickerViewController"
      via: "presentation"
---

<objective>
Implement the "Save As" functionality for Android and iOS platforms.

Purpose: Provide a native "Save to Files" experience on mobile platforms.
Output: Android and iOS implementations of saveFileWithDialog.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-add-loading-animation-and-verify-desktop-documents/07-CONTEXT.md
@composeApp/src/androidMain/kotlin/de/fampopprol/dhbwhorb/util/FileViewer.android.kt
@composeApp/src/iosMain/kotlin/de/fampopprol/dhbwhorb/util/FileViewer.kt

<interfaces>
From composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/util/FileViewer.kt:
```kotlin
expect fun saveFileWithDialog(byteArray: ByteArray, fileName: String)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Android implementation of saveFileWithDialog</name>
  <files>composeApp/src/androidMain/kotlin/de/fampopprol/dhbwhorb/util/FileViewer.android.kt</files>
  <action>
    - Implement `actual fun saveFileWithDialog(byteArray: ByteArray, fileName: String)` in `androidMain/util/FileViewer.android.kt`.
    - Use `Intent.ACTION_CREATE_DOCUMENT`.
    - Set the MIME type to `application/pdf`.
    - Set the extra `Intent.EXTRA_TITLE` with `fileName`.
    - Since getting an activity result in a static `actual fun` is complex without a centralized activity/result mechanism, for now:
      - Start the `ACTION_CREATE_DOCUMENT` activity.
      - If this requires an activity result to write the data, the executor should implement the most feasible way to achieve this within the current architecture (e.g., passing the byte array through a temporary storage if needed, or by ensuring the context used can handle it).
      - If it's too complex to write the data without a persistent activity listener, consider documenting this limitation or using a simpler method like saving to the public "Downloads" folder if permissions allow as a fallback.
    - Reference D-02 in 07-CONTEXT.md.
  </action>
  <verify>
    <automated>grep -q "actual fun saveFileWithDialog" composeApp/src/androidMain/kotlin/de/fampopprol/dhbwhorb/util/FileViewer.android.kt</automated>
  </verify>
  <done>Android users see a system "Save As" dialog when saving documents.</done>
</task>

<task type="auto">
  <name>Task 2: iOS implementation of saveFileWithDialog</name>
  <files>composeApp/src/iosMain/kotlin/de/fampopprol/dhbwhorb/util/FileViewer.kt</files>
  <action>
    - Implement `actual fun saveFileWithDialog(byteArray: ByteArray, fileName: String)` in `iosMain/FileViewer.kt`.
    - Use `UIDocumentPickerViewController` with `UTType.pdf` or equivalent.
    - Export the document (after saving the `byteArray` to a temporary file) by initializing the document picker with the temporary file's URL and `UIDocumentPickerMode.ExportToService`.
    - Present the picker from the root view controller.
    - Ensure temporary files are cleaned up if appropriate.
    - Reference D-02 in 07-CONTEXT.md.
  </action>
  <verify>
    <automated>grep -q "actual fun saveFileWithDialog" composeApp/src/iosMain/kotlin/de/fampopprol/dhbwhorb/util/FileViewer.kt</automated>
  </verify>
  <done>iOS users can select a save location in the Files app.</done>
</task>

</tasks>

<verification>
Check that both platform-specific source sets compile.
</verification>

<success_criteria>
- Android implementation of saveFileWithDialog uses Intent.ACTION_CREATE_DOCUMENT.
- iOS implementation of saveFileWithDialog uses UIDocumentPickerViewController.
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-loading-animation-and-verify-desktop-documents/07-03-SUMMARY.md`
</output>

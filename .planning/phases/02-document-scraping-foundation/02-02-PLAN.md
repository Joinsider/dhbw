---
phase: 02-document-scraping-foundation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [
  "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/DualisDocumentService.kt",
  "composeApp/src/commonTest/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/DualisDocumentServiceTest.kt"
]
autonomous: true
requirements: ["DOC-03"]
must_haves:
  truths:
    - "DualisDocumentService successfully fetches document HTML from network."
    - "The service handles re-authentication for expired sessions automatically."
    - "Documents are correctly parsed using DocumentParser."
    - "Automated tests verify success and re-authentication scenarios using MockEngine."
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/DualisDocumentService.kt"
      provides: "Network fetching and orchestration for documents"
    - path: "composeApp/src/commonTest/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/DualisDocumentServiceTest.kt"
      provides: "Automated unit tests for the document service"
  key_links:
    - from: "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/DualisDocumentService.kt"
      to: "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/parser/DocumentParser.kt"
      via: "dependency"
    - from: "composeApp/src/commonTest/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/DualisDocumentServiceTest.kt"
      to: "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/DualisDocumentService.kt"
      via: "unit testing"
---

<objective>
Implement the Document service to fetch and parse documents from the Dualis portal, handling sessions and re-authentication.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-document-scraping-foundation/research/RESEARCH.md
@composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/DualisGradeService.kt
@composeApp/src/commonTest/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/AuthenticationServiceTest.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement DualisDocumentService</name>
  <files>composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/DualisDocumentService.kt</files>
  <action>
    Create `DualisDocumentService.kt` in `de.fampopprol.dhbwhorb.data.dualis.remote.services`.
    Use the `DualisGradeService.kt` as a blueprint for structure, including:
    - Dependencies: `DualisApiClient`, `SessionManager`, `AuthenticationService`, `DocumentParser`, `HtmlParser`.
    - `fetchDocumentsWithRetry` function to handle session expiration.
    - URL structure from RESEARCH.md: `?APPNAME=CampusNet&PRGNAME=CREATEDOCUMENT&ARGUMENTS=-N{sessionId},-N000339`.
    - Implement the logic to detect session expiration (redirect to login) and trigger `AuthenticationService.reauthenticate()`.
  </action>
  <verify>
    File exists and contains `fetchDocuments` with retry logic.
  </verify>
  <done>
    `DualisDocumentService` implemented with session management.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DualisDocumentServiceTest</name>
  <files>composeApp/src/commonTest/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/DualisDocumentServiceTest.kt</files>
  <behavior>
    - Test successful document fetching and parsing.
    - Test expired session: Mock response returning a login page or redirect, verify it calls reauthenticate and retries.
    - Test failure scenario (network error).
  </behavior>
  <action>
    Create `DualisDocumentServiceTest.kt` using Ktor's `MockEngine`.
    Follow the pattern in `AuthenticationServiceTest.kt`.
    1. Set up `MockEngine` to return different responses based on the request.
    2. Mock the `AuthenticationService` or use the `SessionManager` to simulate an expired session.
    3. Verify that `DualisDocumentService` makes a second request after a session timeout.
  </action>
  <verify>
    <automated>./gradlew :composeApp:testDebugUnitTest --tests "de.fampopprol.dhbwhorb.data.dualis.remote.services.DualisDocumentServiceTest"</automated>
  </verify>
  <done>
    `DualisDocumentService` is fully tested including re-authentication.
  </done>
</task>

</tasks>

<success_criteria>
- `DualisDocumentService` provides a reliable interface for fetching documents.
- Re-authentication logic is verified by automated tests.
- All tests pass in the commonTest module.
</success_criteria>

<output>
After completion, create `.planning/phases/02-document-scraping-foundation/02-02-SUMMARY.md`
</output>

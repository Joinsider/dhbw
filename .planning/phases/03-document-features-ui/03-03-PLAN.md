---
phase: 03-document-features-ui
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [
  "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt",
  "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/pages/DocumentsPage.kt"
]
autonomous: true
requirements: [DOC-UI-02]
must_haves:
  truths:
    - "DocumentsPage includes a search bar at the top of the document list"
    - "Typing in the search bar filters the document list in real-time"
    - "The list updates to show 'No documents found' if the search matches nothing"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt"
      provides: "Search state and filtering logic"
    - path: "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/pages/DocumentsPage.kt"
      provides: "Search bar UI component integration"
  key_links:
    - from: "DocumentsPage"
      to: "DocumentsViewModel.onSearchQueryChange"
      via: "UI event callback"
---

<objective>
Implement search functionality for the document list to satisfy DOC-UI-02.

Purpose: Allow users to quickly find specific documents by title.
Output: Integrated search bar in the UI and filtering logic in the ViewModel.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt
@composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/pages/DocumentsPage.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ViewModel with Search Logic</name>
  <files>composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt</files>
  <action>
    - Update `DocumentsUiState` to include `searchQuery: String = ""`.
    - In `DocumentsViewModel`, add a `_searchQuery` StateFlow.
    - Create a `filteredDocuments` property (or use `combine` on `_documents` and `_searchQuery`) that returns documents matching the query (case-insensitive).
    - Add `onSearchQueryChange(query: String)` method to update the search query.
    - Ensure `uiState` reflects the filtered documents instead of the raw list.
  </action>
  <verify>
    <automated>
      grep "searchQuery" composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt
      grep "onSearchQueryChange" composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt
    </automated>
  </verify>
  <done>ViewModel handles search state and filters documents.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Search UI in DocumentsPage</name>
  <files>composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/pages/DocumentsPage.kt</files>
  <action>
    - Add a `DockedSearchBar` or a simple `OutlinedTextField` with a search icon at the top of the `DocumentsPage` (above the `LazyColumn`).
    - Bind the text value to `uiState.searchQuery`.
    - Bind the `onValueChange` to `viewModel.onSearchQueryChange`.
    - Add a clear button (trailing icon) when the query is not empty.
    - Ensure the layout is responsive and follows Material 3 guidelines.
  </action>
  <verify>
    <automated>
      grep "SearchBar" composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/pages/DocumentsPage.kt || grep "TextField" composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/pages/DocumentsPage.kt
    </automated>
  </verify>
  <done>Search bar is integrated into the Documents page UI.</done>
</task>

</tasks>

<verification>
Manual verification: Open Documents page, type in the search bar, and verify the list filters correctly.
</verification>

<success_criteria>
- [ ] Users can enter text into a search bar on the Documents screen.
- [ ] The document list updates immediately as the user types.
- [ ] Clearing the search bar restores the full document list.
</success_criteria>

<output>
After completion, create `.planning/phases/03-document-features-ui/03-03-SUMMARY.md`
</output>

---
phase: 03-document-features-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/App.kt",
  "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/navigation/BottomNavigationBar.kt",
  "composeApp/src/commonMain/composeResources/values/strings.xml",
  "composeApp/src/commonMain/composeResources/values-de/strings.xml",
  "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt",
  "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/components/DocumentCard.kt"
]
autonomous: true
requirements: [DOC-UI-01, DOC-UI-04]
must_haves:
  truths:
    - "AppScreen enum includes DOCUMENTS"
    - "BottomNavItem enum includes DOCUMENTS with Description icon"
    - "String resources for navigation and documents are available"
    - "DocumentsViewModel exists and implements basic fetching logic using DualisDocumentService"
    - "DocumentCard component exists and displays document metadata"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt"
      provides: "ViewModel for documents page"
    - path: "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/components/DocumentCard.kt"
      provides: "UI component for document list items"
  key_links:
    - from: "DocumentsViewModel"
      to: "DualisDocumentService"
      via: "dependency injection"
---

<objective>
Establish the foundation for the Documents UI by updating navigation enums, adding required resources, and creating the core ViewModel and UI components.

Purpose: Enable navigation to the documents section and provide the data fetching and display logic.
Output: Updated navigation enums, new string resources, DocumentsViewModel, and DocumentCard component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/App.kt
@composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/navigation/BottomNavigationBar.kt
@composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/data/dualis/models/DualisDocument.kt
@composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/data/dualis/remote/services/DualisDocumentService.kt
@composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/grades/viewModels/GradesViewModel.kt

<interfaces>
From DualisDocument.kt:
```kotlin
data class DualisDocument(
    val title: String,
    val date: String,
    val time: String,
    val downloadUrl: String
)
```

From DualisDocumentService.kt:
```kotlin
class DualisDocumentService(...) {
    fun hasCredentialsOrSession(): Boolean
    suspend fun fetchDocuments(): Result<List<DualisDocument>>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navigation and Resource Foundation</name>
  <files>
    composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/App.kt,
    composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/navigation/BottomNavigationBar.kt,
    composeApp/src/commonMain/composeResources/values/strings.xml,
    composeApp/src/commonMain/composeResources/values-de/strings.xml
  </files>
  <action>
    - Add `DOCUMENTS` to `AppScreen` enum in `App.kt`.
    - Add `DOCUMENTS(Icons.Default.Description)` to `BottomNavItem` enum in `BottomNavigationBar.kt`. 
    - Update `getNavText` in `BottomNavigationBar.kt` to return `stringResource(Res.string.nav_documents)` for `BottomNavItem.DOCUMENTS`.
    - Add the following string resources to `strings.xml` (both `values` and `values-de`):
      - `nav_documents`: "Documents" / "Dokumente"
      - `documents`: "Documents" / "Dokumente"
      - `login_required_for_documents`: "Login required to view documents" / "Login erforderlich, um Dokumente anzuzeigen"
      - `no_documents_found`: "No documents found" / "Keine Dokumente gefunden"
    - Update `App` composable in `App.kt` to initialize `DualisDocumentService` and `DocumentsViewModel` similar to `GradesViewModel`.
  </action>
  <verify>
    <automated>
      # Check if AppScreen.DOCUMENTS is present
      grep "DOCUMENTS" composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/App.kt
      # Check if BottomNavItem.DOCUMENTS is present
      grep "DOCUMENTS" composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/navigation/BottomNavigationBar.kt
      # Check if strings are present
      grep "nav_documents" composeApp/src/commonMain/composeResources/values/strings.xml
    </automated>
  </verify>
  <done>Navigation enums and string resources updated; ViewModel initialization added to App.kt.</done>
</task>

<task type="auto">
  <name>Task 2: Documents ViewModel and Card Component</name>
  <files>
    composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt,
    composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/components/DocumentCard.kt
  </files>
  <action>
    - Create `DocumentsViewModel.kt` in `de.fampopprol.dhbwhorb.ui.documents.viewModels`.
    - Implement `DocumentsUiState` data class (isLoading, isRefreshing, documents, error, requiresLogin).
    - Implement `DocumentsViewModel` class following `GradesViewModel` pattern, using `DualisDocumentService`.
    - Implement `loadDocuments()` and `refreshDocuments()` methods.
    - Create `DocumentCard.kt` in `de.fampopprol.dhbwhorb.ui.documents.components`.
    - `DocumentCard` should be a `Card` displaying `DualisDocument.title`, `date`, and `time`. Use `Icons.Default.Description` as a leading icon if it fits the design.
  </action>
  <verify>
    <automated>
      # Check if files exist
      ls composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt
      ls composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/components/DocumentCard.kt
      # Check for basic content
      grep "class DocumentsViewModel" composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt
      grep "fun DocumentCard" composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/components/DocumentCard.kt
    </automated>
  </verify>
  <done>DocumentsViewModel and DocumentCard component implemented.</done>
</task>

</tasks>

<verification>
Check if navigation enums and string resources are correctly integrated and if the new components follow existing patterns.
</verification>

<success_criteria>
- [ ] Navigation enums `AppScreen` and `BottomNavItem` updated with `DOCUMENTS`.
- [ ] String resources added for navigation and document status messages.
- [ ] `DocumentsViewModel` correctly fetches documents via `DualisDocumentService`.
- [ ] `DocumentCard` displays document title and date/time.
</success_criteria>

<output>
After completion, create `.planning/phases/03-document-features-ui/03-01-SUMMARY.md`
</output>

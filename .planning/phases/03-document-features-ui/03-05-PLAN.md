---
phase: 03-document-features-ui
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified: [
  "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt",
  "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/components/DocumentCard.kt"
]
autonomous: true
requirements: [DOC-UI-03]
must_haves:
  truths:
    - "Each document in the list has a clickable download icon"
    - "Clicking the icon triggers a download and opens the file"
    - "ViewModel handles the downloading state"
  artifacts:
    - path: "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt"
      provides: "Download action handling"
    - path: "composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/components/DocumentCard.kt"
      provides: "Download icon and click listener"
---

<objective>
Integrate document downloading and viewing into the UI.

Purpose: Provide the user with a way to trigger downloads and view documents.
Output: Updated DocumentCard with download action and ViewModel with download handling logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt
@composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/components/DocumentCard.kt
@composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/util/FileViewer.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ViewModel for Download Action</name>
  <files>composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt</files>
  <action>
    - Add `isDownloading` state (or map document IDs to download status).
    - Add `downloadAndOpenDocument(document: DualisDocument)` method.
    - Implementation:
      - Set loading state.
      - Call `dualisDocumentService.downloadDocument(document.downloadUrl)`.
      - On success, call `FileViewer.openFile(bytes, document.title + extension)`.
      - On failure, show an error message.
      - Reset loading state.
  </action>
  <verify>
    <automated>
      grep "downloadAndOpenDocument" composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/viewModels/DocumentsViewModel.kt
    </automated>
  </verify>
  <done>ViewModel handles document download and viewing requests.</done>
</task>

<task type="auto">
  <name>Task 2: Update DocumentCard with Download Icon</name>
  <files>composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/components/DocumentCard.kt</files>
  <action>
    - Update `DocumentCard` to include an `IconButton` with `Icons.Default.Download`.
    - Add `onDownloadClick: () -> Unit` parameter to `DocumentCard`.
    - Update `DocumentsPage` to pass `viewModel.downloadAndOpenDocument(document)` to `DocumentCard`.
    - (Optional) Disable the button or show a spinner if that specific document is being downloaded.
  </action>
  <verify>
    <automated>
      grep "Icons.Default.Download" composeApp/src/commonMain/kotlin/de/fampopprol/dhbwhorb/ui/documents/components/DocumentCard.kt
    </automated>
  </verify>
  <done>DocumentCard UI updated with download functionality.</done>
</task>

</tasks>

<verification>
Manual verification: Open Documents page, click the download icon on a document, and verify it opens in the platform's native viewer.
</verification>

<success_criteria>
- [ ] Users can click a download icon on any document item.
- [ ] The app downloads the file and opens it automatically.
- [ ] Error messages are shown if the download fails.
</success_criteria>

<output>
After completion, create `.planning/phases/03-document-features-ui/03-05-SUMMARY.md`
</output>
